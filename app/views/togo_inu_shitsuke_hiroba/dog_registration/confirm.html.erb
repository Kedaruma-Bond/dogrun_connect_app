<% content_for(:title, t('.title')) %>
<div class="max-w-5xl mx-auto px-6 sm:px-6 lg:px-8 mt-5">
  <div class="container bg-purple-200 w-full shadow rounded p-8 sm:p-10">
    <%= render 'togo_inu_shitsuke_hiroba/dog_registration/dog_registration_confirm' %>
    <%= simple_form_for @dog_registration, url: togo_inu_shitsuke_hiroba_dog_registration_path, data: { turbo: false } do |f| %>
      <div class="mt-6 sm:flex justify-center">
        <div class="mt-4 shadow sm:mx-3 sm:mt-0">
          <%= f.button :submit, t('defaults.back'), name: 'back', class: "items-center w-full px-10 py-4 text-base font-semibold text-white bg-red-700 rounded hover:bg-red-600 focus:ring-2 focus:ring-offset-2 focus:ring-blue-700 focus:outline-none" %>
        </div>
        <div class="shadow sm:mx-3 sm:mt-0 mt-4">
          <%= f.button :submit, t('defaults.registration'), class: "items-center w-full px-10 py-4 text-base font-semibold text-white bg-green-700 rounded hover:bg-green-600 focus:ring-2 focus:ring-offset-2 focus:ring-blue-700 focus:outline-none" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
