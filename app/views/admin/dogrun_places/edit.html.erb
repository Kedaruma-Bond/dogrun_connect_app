<% content_for(:title, t('.title')) %>
<div class="bg-purple-200 w-full shadow rounded p-8 sm:p-10">
  <p class="text-3xl text-gray-700 font-bold leading-7 text-center mb-10 mt-6"><%= t'.title' %></p>
  <%= simple_form_for @dogrun_place, url: admin_dogrun_place_path, data: { controller: 'preview disable', "disable-with-value": t('defaults.updating'), turbo_frame: "_top" } do |f| %>
    <%= render partial: 'admin/dogrun_places/form', locals: { f: f } %>
  <% end %>
</div>

