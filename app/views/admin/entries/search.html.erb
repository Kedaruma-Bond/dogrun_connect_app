<% content_for(:title, t('.title')) %>
<div class="container bg-gray-300 flex flex-col">
  <h2 class="mt-2 ml-2">
    <%= t('.title') %>
  </h2>
  <div class="overflow-x-auto -mx-8">
    <div class="py-2 inline-block w-full px-8">
      <div class="overflow-hidden">
        <%= render partial: 'admin/entries/search_form' %>
        <h2 class="text-gray-700 text-sm flex items-end justify-end mr-7 mt-3">
          <%= t('.search_results_is')%>
          <span class="text-xl mr-2 ml-2">
            <%= @entries_results.nil? ? 0 : @entries_results.total_count %> 
          </span>
          <%= t('.search_results_unit') %>
        </h2>
        <table class="mt-3 w-full">
          <thead class="bg-white border-b">
            <tr>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left underline">
                <%= sort_link(@q, :entry_at, t('defaults.date')) %>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left">
                <%= t('defaults.registration_number') %>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left underline">
                <%= sort_link(@q, :dog_user_name, t('defaults.name')) %>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left underline">
                <%= sort_link(@q, :dog_name, t('defaults.dog_name')) %>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left underline">
                <%= sort_link(@q, :entry_at, t('defaults.entry_at')) %>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left underline">
                <%= sort_link(@q, :exit_at, t('defaults.exit_at')) %>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-900 px-6 py-4 text-left underline">
                <%= t('defaults.delete') %>
              </th>
            </tr>
          </thead>
          <tbody>
            <%= render @entries_results %>
          </tbody>
        </table>
      </div>
      <%= paginate @entries_results %>
    </div>
  </div>
</div>
