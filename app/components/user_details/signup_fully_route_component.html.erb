<% content_for(:title, @title) %>
<div class="max-w-5xl md:mx-auto px-2 pb-2 sm:mx-32 bg-purple-200 rounded-lg">
  <div class="w-full px-4 py-6 sm:p-10">
    <p class="text-2xl font-bold text-center text-gray-700 mb-3">
      <%= @title %>
    </p>
    <%= content %>
  </div>
</div>
